void AA(){
  myservoA.write(A_Pos_raus);    // A
  myservoB.write(B_Pos_raus);
  myservoC.write(C_Pos_raus);
  myservoD.write(D_Pos_rein);
  myservoE.write(E_Pos_raus);
  myservoF.write(F_Pos_raus);
  myservoG.write(G_Pos_raus);
  LEDsterunern();
}

void a(){
  myservoA.write(A_Pos_raus);    // a
  myservoB.write(B_Pos_raus);
  myservoC.write(C_Pos_raus);
  myservoD.write(D_Pos_raus);
  myservoE.write(E_Pos_raus);
  myservoF.write(F_Pos_rein);
  myservoG.write(G_Pos_raus);
  LEDsterunern();
}

void b(){
  myservoA.write(A_Pos_rein);    // b
  myservoB.write(B_Pos_rein);
  myservoC.write(C_Pos_raus);
  myservoD.write(D_Pos_raus);
  myservoE.write(E_Pos_raus);
  myservoF.write(F_Pos_raus);
  myservoG.write(G_Pos_raus);
  LEDsterunern();
}

void CC(){
  myservoA.write(A_Pos_raus);    // C
  myservoB.write(B_Pos_rein);
  myservoC.write(C_Pos_rein);
  myservoD.write(D_Pos_raus);
  myservoE.write(E_Pos_raus);
  myservoF.write(F_Pos_raus);
  myservoG.write(G_Pos_rein);
  LEDsterunern();
}

void c(){
  myservoA.write(A_Pos_rein);    // c
  myservoB.write(B_Pos_rein);
  myservoC.write(C_Pos_rein);
  myservoD.write(D_Pos_raus);
  myservoE.write(E_Pos_raus);
  myservoF.write(F_Pos_rein);
  myservoG.write(G_Pos_raus);
  LEDsterunern();
}

void d(){
  myservoA.write(A_Pos_rein);    // d
  myservoB.write(B_Pos_raus);
  myservoC.write(C_Pos_raus);
  myservoD.write(D_Pos_raus);
  myservoE.write(E_Pos_raus);
  myservoF.write(F_Pos_rein);
  myservoG.write(G_Pos_raus);
  LEDsterunern();
}

void EE(){
  myservoA.write(A_Pos_raus);    // E
  myservoB.write(B_Pos_rein);
  myservoC.write(C_Pos_rein);
  myservoD.write(D_Pos_raus);
  myservoE.write(E_Pos_raus);
  myservoF.write(F_Pos_raus);
  myservoG.write(G_Pos_raus);
  LEDsterunern();
}

void FF(){
  myservoA.write(A_Pos_raus);    // F
  myservoB.write(B_Pos_rein);
  myservoC.write(C_Pos_rein);
  myservoD.write(D_Pos_rein);
  myservoE.write(E_Pos_raus);
  myservoF.write(F_Pos_raus);
  myservoG.write(G_Pos_raus);
  LEDsterunern();
}

void GG(){
  myservoA.write(A_Pos_raus);    // G
  myservoB.write(B_Pos_rein);
  myservoC.write(C_Pos_raus);
  myservoD.write(D_Pos_raus);
  myservoE.write(E_Pos_raus);
  myservoF.write(F_Pos_raus);
  myservoG.write(G_Pos_rein);
  LEDsterunern();
}

void H(){
  myservoA.write(A_Pos_rein);    // H
  myservoB.write(B_Pos_raus);
  myservoC.write(C_Pos_raus);
  myservoD.write(D_Pos_rein);
  myservoE.write(E_Pos_raus);
  myservoF.write(F_Pos_raus);
  myservoG.write(G_Pos_raus);
  LEDsterunern();
}

void h(){
  myservoA.write(A_Pos_rein);    // h
  myservoB.write(B_Pos_rein);
  myservoC.write(C_Pos_raus);
  myservoD.write(D_Pos_rein);
  myservoE.write(E_Pos_raus);
  myservoF.write(F_Pos_raus);
  myservoG.write(G_Pos_raus);
  LEDsterunern();
}

void I(){
  myservoA.write(A_Pos_rein);    // I
  myservoB.write(B_Pos_raus);
  myservoC.write(C_Pos_raus);
  myservoD.write(D_Pos_rein);
  myservoE.write(E_Pos_rein);
  myservoF.write(F_Pos_rein);
  myservoG.write(G_Pos_rein);
  LEDsterunern();
}

void i(){
  myservoA.write(A_Pos_raus);    // i
  myservoB.write(B_Pos_rein);
  myservoC.write(C_Pos_rein);
  myservoD.write(D_Pos_raus);
  myservoE.write(E_Pos_raus);
  myservoF.write(F_Pos_rein);
  myservoG.write(G_Pos_rein);
  LEDsterunern();
}

void J(){
  myservoA.write(A_Pos_rein);    // J
  myservoB.write(B_Pos_raus);
  myservoC.write(C_Pos_raus);
  myservoD.write(D_Pos_raus);
  myservoE.write(E_Pos_raus);
  myservoF.write(F_Pos_rein);
  myservoG.write(G_Pos_rein);
  LEDsterunern();
}

void j(){
  myservoA.write(A_Pos_raus);    // j
  myservoB.write(B_Pos_rein);
  myservoC.write(C_Pos_raus);
  myservoD.write(D_Pos_raus);
  myservoE.write(E_Pos_rein);
  myservoF.write(F_Pos_rein);
  myservoG.write(G_Pos_rein);
  LEDsterunern();
}

void L(){
  myservoA.write(A_Pos_rein);    // L
  myservoB.write(B_Pos_rein);
  myservoC.write(C_Pos_rein);
  myservoD.write(D_Pos_raus);
  myservoE.write(E_Pos_raus);
  myservoF.write(F_Pos_raus);
  myservoG.write(G_Pos_rein);
  LEDsterunern();
}

void l(){
  myservoA.write(A_Pos_rein);    // l
  myservoB.write(B_Pos_raus);
  myservoC.write(C_Pos_raus);
  myservoD.write(D_Pos_rein);
  myservoE.write(E_Pos_rein);
  myservoF.write(F_Pos_rein);
  myservoG.write(G_Pos_rein);
  LEDsterunern();
}

void N(){
  myservoA.write(A_Pos_raus);    // N
  myservoB.write(B_Pos_raus);
  myservoC.write(C_Pos_raus);
  myservoD.write(D_Pos_rein);
  myservoE.write(E_Pos_raus);
  myservoF.write(F_Pos_raus);
  myservoG.write(G_Pos_rein);
  LEDsterunern();
}

void n(){
  myservoA.write(A_Pos_rein);    // n
  myservoB.write(B_Pos_rein);
  myservoC.write(C_Pos_raus);
  myservoD.write(D_Pos_rein);
  myservoE.write(E_Pos_raus);
  myservoF.write(F_Pos_rein);
  myservoG.write(G_Pos_raus);
  LEDsterunern();
}

void O(){
  myservoA.write(A_Pos_raus);  // O
  myservoB.write(B_Pos_raus);
  myservoC.write(C_Pos_raus);
  myservoD.write(D_Pos_raus);
  myservoE.write(E_Pos_raus);
  myservoF.write(F_Pos_raus);
  myservoG.write(G_Pos_rein);
  LEDsterunern();
}

void o(){
  myservoA.write(A_Pos_rein);    // o
  myservoB.write(B_Pos_rein);
  myservoC.write(C_Pos_raus);
  myservoD.write(D_Pos_raus);
  myservoE.write(E_Pos_raus);
  myservoF.write(F_Pos_rein);
  myservoG.write(G_Pos_raus);
  LEDsterunern();
}

void P(){
  myservoA.write(A_Pos_raus);    // P
  myservoB.write(B_Pos_raus);
  myservoC.write(C_Pos_rein);
  myservoD.write(D_Pos_rein);
  myservoE.write(E_Pos_raus);
  myservoF.write(F_Pos_raus);
  myservoG.write(G_Pos_raus);
  LEDsterunern();
}

void q(){
  myservoA.write(A_Pos_raus);    // q
  myservoB.write(B_Pos_raus);
  myservoC.write(C_Pos_raus);
  myservoD.write(D_Pos_rein);
  myservoE.write(E_Pos_rein);
  myservoF.write(F_Pos_raus);
  myservoG.write(G_Pos_raus);
  LEDsterunern();
}

void r(){
  myservoA.write(A_Pos_rein);    // r
  myservoB.write(B_Pos_rein);
  myservoC.write(C_Pos_rein);
  myservoD.write(D_Pos_rein);
  myservoE.write(E_Pos_raus);
  myservoF.write(F_Pos_rein);
  myservoG.write(G_Pos_raus);
  LEDsterunern();
}

void S(){
  myservoA.write(A_Pos_raus);    // S
  myservoB.write(B_Pos_rein);
  myservoC.write(C_Pos_raus);
  myservoD.write(D_Pos_raus);
  myservoE.write(E_Pos_rein);
  myservoF.write(F_Pos_raus);
  myservoG.write(G_Pos_raus);
  LEDsterunern();
}

void t(){
  myservoA.write(A_Pos_rein);    // t
  myservoB.write(B_Pos_rein);
  myservoC.write(C_Pos_rein);
  myservoD.write(D_Pos_raus);
  myservoE.write(E_Pos_raus);
  myservoF.write(F_Pos_raus);
  myservoG.write(G_Pos_raus);
  LEDsterunern();
}

void U(){
  myservoA.write(A_Pos_rein);    // U
  myservoB.write(B_Pos_raus);
  myservoC.write(C_Pos_raus);
  myservoD.write(D_Pos_raus);
  myservoE.write(E_Pos_raus);
  myservoF.write(F_Pos_raus);
  myservoG.write(G_Pos_rein);
  LEDsterunern();
}

void u(){
  myservoA.write(A_Pos_rein);    // u
  myservoB.write(B_Pos_rein);
  myservoC.write(C_Pos_raus);
  myservoD.write(D_Pos_raus);
  myservoE.write(E_Pos_rein);
  myservoF.write(F_Pos_raus);
  myservoG.write(G_Pos_rein);
  LEDsterunern();
}

void X(){
  myservoA.write(A_Pos_rein);    // X
  myservoB.write(B_Pos_raus);
  myservoC.write(C_Pos_raus);
  myservoD.write(D_Pos_rein);
  myservoE.write(E_Pos_raus);
  myservoF.write(F_Pos_raus);
  myservoG.write(G_Pos_raus);
  LEDsterunern();
}

void Y(){
  myservoA.write(A_Pos_rein);    // Y
  myservoB.write(B_Pos_raus);
  myservoC.write(C_Pos_raus);
  myservoD.write(D_Pos_raus);
  myservoE.write(E_Pos_rein);
  myservoF.write(F_Pos_raus);
  myservoG.write(G_Pos_raus);
  LEDsterunern();
}

void Z(){
  myservoA.write(A_Pos_raus);    // Z
  myservoB.write(B_Pos_raus);
  myservoC.write(C_Pos_rein);
  myservoD.write(D_Pos_raus);
  myservoE.write(E_Pos_raus);
  myservoF.write(F_Pos_rein);
  myservoG.write(G_Pos_raus);
  LEDsterunern();
}

void oe(){
  myservoA.write(A_Pos_raus);    // ö
  myservoB.write(B_Pos_rein);
  myservoC.write(C_Pos_raus);
  myservoD.write(D_Pos_raus);
  myservoE.write(E_Pos_raus);
  myservoF.write(F_Pos_rein);
  myservoG.write(G_Pos_raus);
  LEDsterunern();
}

void ue(){
  myservoA.write(A_Pos_raus);    // ü
  myservoB.write(B_Pos_rein);
  myservoC.write(C_Pos_raus);
  myservoD.write(D_Pos_raus);
  myservoE.write(E_Pos_raus);
  myservoF.write(F_Pos_rein);
  myservoG.write(G_Pos_rein);
  LEDsterunern();
}
